# Built-in Components

NBCL by default comes with only one `component` (i.e node), that being the `Object`. 

## `Object` properties

- **type name**: `Object`
- **id enforced**: `false`
- **validation**: `PropValidation::Loose`
- **child_count**: `None` (infinite)